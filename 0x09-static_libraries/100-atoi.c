#include "main.h"
#include<stdio.h>
/**
 * _atoi - the entry for the program
 * @s: number of char byte to be copied
 * Return: the number of len on sucess
 */
int _atoi(char *s)
{
	*s=0;
	return (0);
}
