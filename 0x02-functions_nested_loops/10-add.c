#include<stdio.h>
/**
 * add - function to check if character is minus or pls
 * @n: the character to check
 * Return: 0 always
 */
int add(int a, int b)
{
	int thelast = a + b;

	return (thelast);
}
