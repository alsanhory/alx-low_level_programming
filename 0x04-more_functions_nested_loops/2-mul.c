#include<stdio.h>
/**
 * mul - function to check if character is digit
 * @a: first number
 * @b: second number
 * Return: the result always
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
